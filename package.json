{
  "name": "akb-fee-collection",
  "version": "1.0.0",
  "private": true,
  "description": "AKB School fee collection app — SPA with a Node server (shared state, Excel export, weekly email, WhatsApp reminders)",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/setup.js && node server.js",
    "db:migrate": "node scripts/migrate.js",
    "db:seed": "node scripts/seed.js",
    "db:setup": "node scripts/setup.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "mysql2": "^3.11.0",
    "nodemailer": "^6.9.14"
  }
}
